<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="view_network_textview">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:gravity">center_vertical|left</item>
    </style>

    <style name="view_network_textview_psk" parent="view_network_textview">
        <item name="android:layout_height">@dimen/height_view_network_textview_psk</item>
        <item name="android:textColor">#88000000</item>
        <item name="android:textSize">16dp</item>
    </style>

    <style name="view_network_textview_ssid" parent="view_network_textview">
        <item name="android:layout_height">@dimen/height_view_network_textview_ssid</item>
        <item name="android:textColor">#aa000000</item>
        <item name="android:textSize">18dp</item>
    </style>

    <style name="divider">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/height_divider</item>
        <item name="android:background">@color/color_gray_divider</item>
    </style>

</resources>
